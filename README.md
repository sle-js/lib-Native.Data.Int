# lib-Native.Data.Int

Idiomatic `sle` native wrapper of JavaScript integers.  The functions within the package have the following characteristics:

* They never throw an exception,
* They never mutate their parameters, and
* The never return null or undefined 